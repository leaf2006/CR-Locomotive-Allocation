[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
license = "AGPL-3.0-or-later"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "orjson>=3.11.9",
    "requests>=2.33.1",
]
